# MongoDB Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitnessduel-mongo
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fitnessduel-mongo
  template:
    metadata:
      labels:
        app: fitnessduel-mongo
    spec:
      containers:
      - name: mongo
        image: mongo:7.0
        ports:
          - containerPort: 27017
        env:
          - name: MONGO_INITDB_ROOT_USERNAME
            value: "root"
          - name: MONGO_INITDB_ROOT_PASSWORD
            value: "example"
            
