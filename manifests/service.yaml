apiVersion: v1
kind: Service
metadata:
  name: fitnessduel-service
  namespace: default
spec:
  selector:
    app: fitnessduel
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
